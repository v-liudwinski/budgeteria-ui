# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Status

This repository is in its initial state. Only a Visual Studio-style `.gitignore` and a blank `README.md` exist — no framework, build system, or source code has been added yet.

## Project Name

`budgeteria-ui` — a budgeting UI application (framework/stack TBD).

## Notes

- The `.gitignore` is the standard Visual Studio template, suggesting a .NET-based stack (e.g., Blazor, ASP.NET, MAUI) may be intended, but this has not been confirmed.
- Once a framework is chosen and scaffolded, update this file with build, lint, and test commands.
